import type { ColorType, SizeType } from '@/types/elements'

// =================== Colors ===================
export const COLORS: Record<ColorType, ColorType> = {
  none: 'none',
  primary: 'primary',
  error: 'error',
}

// =================== Sizes ===================
export const SIZES: Record<SizeType, SizeType> = {
  xs: 'xs',
  sm: 'sm',
  md: 'md',
  lg: 'lg',
  xl: 'xl',
}
